# Build outputs
target/
build/
out/
*.class
*.jar

# IDE
.idea/
*.iml
.vscode/
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db

# Local config / secrets — never commit API keys
**/application-local.yml
**/application-local*.yml
**/.env
**/.env.*

# Logs
*.log
logs/

# Maven
.mvn/wrapper/maven-wrapper.jar

# Claude / AI session state
.claude/
CLAUDE.md

# Local deploy output (mvn install target)
current/

# Runtime artifacts from demo endpoints (generated MP3 / video files)
agentscope-output/

# WSL+Windows path-corruption residue: when maven was pointed at a Windows
# local repo on /mnt/d, WSL treated backslashes as part of the filename and
# created a literal directory here. Safe to delete if it ever resurfaces.
D:\\software_install\\maven\\maven-repository-3.9.11/
