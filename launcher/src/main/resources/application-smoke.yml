# Smoke-test profile overlay. Activated via --spring.profiles.active=smoke.
#
# The base application.yml already enables the minimax provider and the
# ai-promo demo endpoint, so this overlay is intentionally a no-op kept for
# backwards compatibility. Operators can use it to flip on additional
# providers (openai / dashscope) without editing the base file.
agentscope: {}
